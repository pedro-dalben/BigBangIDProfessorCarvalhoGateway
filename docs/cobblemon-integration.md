# Integração Cobblemon

A integração é opcional e foi compilada contra Cobblemon 1.7.3. A coleta copia a equipe e os contadores confiáveis da Pokédex no contexto do servidor; referências mutáveis não atravessam o executor HTTP. Sem Cobblemon, o snapshot informa `available: false`.
